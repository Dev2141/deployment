# Vidzai Backend - Production Ready
# Python 3.9+ required

# ── Flask Backend ──────────────────────────────────────────────────────────
flask==3.0.0
flask-cors==4.0.0
gunicorn==21.2.0

# ── Core Dependencies ──────────────────────────────────────────────────────
# Neo4j (for graph database)
neo4j==5.14.0

# Pinecone (for vector database) - use latest stable
pinecone==5.0.1

# HTTP client
requests==2.31.0

# Environment variables
python-dotenv==1.0.0
