# Milestone 2 & 3 — Entity Extraction, Relationship Extraction & RAG
# Python >= 3.9 required (compatible with Render)

# ── Flask Backend ──────────────────────────────────────────────────────────
flask>=2.3.0
flask-cors>=4.0.0
gunicorn>=21.0.0

# Neo4j
neo4j>=5.0,<6.0

# Environment variables
python-dotenv>=1.0.0

# HTTP client for OpenRouter API (Milestone 2)
requests>=2.28.0

# ── Milestone 3: RAG Pipeline ─────────────────────────────────────────────────

# Pinecone vector store + hosted embeddings (llama-text-embed-v2)
pinecone>=3.0.0
langchain-pinecone>=0.0.1

# LangChain core + text splitters
langchain-core>=0.1.0
langchain-text-splitters>=0.0.1

# Groq LLM (llama-3.3-70b-versatile)
langchain-groq>=0.0.1

# Data loading
pandas>=2.0.0

# Jupyter
jupyter>=1.0.0
ipykernel>=6.0.0
